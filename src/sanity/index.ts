import { blogSchema } from './schemas/blog';
import { treatmentSchema } from './schemas/treatment';
import { testimonialSchema } from './schemas/testimonial';

export const schemaTypes = [blogSchema, treatmentSchema, testimonialSchema];
